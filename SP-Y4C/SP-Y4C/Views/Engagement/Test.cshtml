@model SP_Y4C.ViewModels.SurveyEngagementViewModel
@{
    ViewData["Title"] = "Test";
}

<h2>Test</h2>
@{
    foreach (var q in Model.Question)
    {
        <label for=@q.Id>
            <label>@q.Text</label>

        @{ foreach (var c in Model.Choice)
            {
                if (q.Id == c.QuestionId)
                {
                <label for=@c.Id>
                    <label>@c.Text</label>
                    <label>@q.TypeId</label>
                    <input type=@q.TypeId />
                </label>

                    }
                }
            }
            </label>
    }
}
